// Deployment configuration. WORKER_URL is filled in after the first
// `wrangler deploy` (e.g. "https://dualuse-classifier.<account>.workers.dev").
window.CLASSIFIER_CONFIG = {
  WORKER_URL: "REPLACE_AFTER_WORKER_DEPLOY",
  ANNEX_URL: "https://rikiosso.github.io/exports-watch/data/annex.json",
};
